MetaTrader5>=5.0.45
fastapi>=0.111.0
# [standard] pulls in `websockets` — without it uvicorn rejects every
# /stream/ticks connection ("No supported WebSocket library detected").
uvicorn[standard]>=0.30.0
websockets>=12.0
pydantic>=2.7.0
httpx>=0.27.0
python-dotenv>=1.0.0
